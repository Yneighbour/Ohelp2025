# Ohelp2025 前端项目

<div align="center">

## 🏥 智慧养老服务管理系统

基于Vue 3 + Vite构建的现代化老年人服务管理系统前端应用

[![Vue](https://img.shields.io/badge/Vue-3.5.24-4FC08D?style=flat-square&logo=vue.js)](https://vuejs.org/)
[![Vite](https://img.shields.io/badge/Vite-7.3.1-646CFF?style=flat-square&logo=vite)](https://vitejs.org/)
[![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-F7DF1E?style=flat-square&logo=javascript)](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
[![Responsive](https://img.shields.io/badge/Responsive-Yes-00D4AA?style=flat-square)](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Responsive/responsive_design_building_blocks)

</div>

## 📋 项目概述

Ohelp2025是一款专为老年人服务设计的智慧养老服务平台前端管理系统，严格遵循"**后端即宪法**"的设计原则，确保前端完全基于后端API接口进行开发。

## 🧭 角色与路由（重要约束）

本项目前端只允许使用 3 个“宏角色”，用于路由分区与布局分区：

- `ADMIN`
- `WORKER`
- `USER`（统一指代“老人/家属视角”，老人/家属是业务实体，不是角色）

登录接口不返回 role，role 只能来自后端 `User.role`，取值只可能是：`admin` / `manager` / `staff` / `user`。

前端必须在登录成功后进行角色归一化映射：

- `admin` -> `ADMIN`
- `manager` -> `WORKER`
- `staff` -> `WORKER`
- `user` -> `USER`

本地只允许保存宏角色：

- `localStorage['role'] = 'ADMIN' | 'WORKER' | 'USER'`

路由结构按宏角色分区：

- `/login`：唯一入口
- `/admin/**`：管理员域
- `/worker/**`：工作人员域
- `/app/**`：老人/家属域（仅“入口卡片 + 少量详情页”，不做管理型 CRUD）

### 🎯 核心特性

- **🏛️ 严格架构**：严格按照后端9个业务模块进行前端开发
- **👴 适老化设计**：大字体、大按钮、紫色主题、简洁直观的操作界面
- **📱 响应式布局**：完美适配桌面端和移动端设备
- **♿ 无障碍访问**：完整支持键盘导航和屏幕阅读器
- **📊 数据可视化**：内置5种图表类型的数据展示组件
- **🎨 现代化UI**：基于CSS变量的完整设计系统
- **⚡ 高性能**：Vite构建，快速加载和流畅交互
- **🔧 组件化**：10+个可复用UI组件库

## 🛠️ 技术栈

### 核心框架
- **Vue 3.5.24** - 渐进式JavaScript框架
- **Vite 7.3.1** - 下一代前端构建工具
- **Vue Router 4.4.0** - 官方路由管理器

### HTTP & API
- **Axios 1.7.2** - HTTP客户端，支持请求/响应拦截
- **模块化API** - 每个后端模块对应独立API文件

### UI & 样式
- **自定义CSS变量系统** - 紫色主题设计
- **组件化样式架构** - BEM命名规范
- **响应式设计** - 移动优先的适配方案

### 开发工具
- **ESLint** - 代码质量检查
- **Prettier** - 代码格式化
- **HMR** - 热模块替换开发体验

## 📁 项目结构

```
o-f/
├── public/                    # 静态资源目录
├── src/
│   ├── components/            # 组件库
│   │   ├── base/             # 基础组件
│   │   │   ├── BaseCard.vue      # 卡片容器
│   │   │   ├── BaseButton.vue    # 按钮组件
│   │   │   ├── BaseInput.vue     # 输入框组件
│   │   │   └── BaseTable.vue     # 表格组件
│   │   ├── common/            # 通用组件
│   │   │   ├── ChartCanvas.vue       # 图表组件
│   │   │   ├── MessageToast.vue      # 消息提示
│   │   │   ├── PageTransition.vue    # 页面过渡
│   │   │   ├── LoadingIndicator.vue  # 加载指示器
│   │   │   └── HealthScore.vue       # 健康评分
│   │   └── form/              # 表单组件
│   │       └── FormValidator.vue     # 表单验证
│   ├── modules/               # 业务模块层
│   │   ├── auth/             # 认证管理模块
│   │   │   └── loginSuccess.js   # 登录成功处理示例（token+role归一化+跳转）
│   │   ├── user/             # 用户管理模块
│   │   ├── elder/            # 老人信息模块
│   │   ├── activity/         # 活动管理模块
│   │   ├── emergency/        # 紧急求助模块
│   │   ├── health/           # 健康管理模块
│   │   ├── serviceorder/     # 服务订单模块
│   │   ├── worker/           # 工作人员模块
│   │   ├── file/             # 文件管理模块
│   │   └── app/               # 老人/家属端（/app/**）页面
│   │   └── Dashboard.vue     # 系统概览仪表板
│   ├── layouts/               # 角色域布局
│   │   └── AppLayout.vue      # USER 端布局（顶部标题 + 底部导航）
│   ├── router/               # 路由配置
│   │   └── index.js          # Vue Router配置
│   ├── shared/               # 共享工具层
│   │   ├── http.js           # Axios配置和拦截器
│   │   ├── message.js        # 全局消息服务
│   │   ├── role.js           # 后端 role -> 前端宏角色（归一化）
│   │   └── directives.js     # 自定义指令库
│   ├── styles/               # 样式系统
│   │   ├── themes/           # 主题配置
│   │   │   └── variables.css # CSS变量定义
│   │   ├── common.css        # 通用样式
│   │   └── components/       # 组件样式
│   ├── App.vue               # 主应用组件
│   └── main.js               # 应用入口文件
├── index.html                # HTML模板
├── package.json              # 项目配置
├── vite.config.js            # Vite配置
└── README.md                 # 项目文档
```

## 🎨 设计系统

### 颜色系统
```css
/* 主色调 - 紫色系 */
--primary-color: #7C3AED;        /* 主要紫色 */
--primary-light: #A78BFA;       /* 浅紫色 */
--primary-dark: #5B21B6;        /* 深紫色 */

/* 功能色 */
--success-color: #10B981;       /* 成功绿 */
--warning-color: #F59E0B;       /* 警告橙 */
--error-color: #EF4444;         /* 错误红 */
--info-color: #7C3AED;          /* 信息紫 */

/* 中性色 */
--text-primary: #1F2937;        /* 主要文字 */
--text-secondary: #6B7280;      /* 次要文字 */
--background-color: #F9FAFB;    /* 背景色 */
```

### 字体系统
```css
/* 适老化字体设计 */
--font-size-xs: 12px;     /* 小文字 */
--font-size-sm: 14px;     /* 小标题 */
--font-size-base: 16px;   /* 正文字体 */
--font-size-lg: 18px;     /* 大标题 */
--font-size-xl: 20px;     /* 页面标题 */
--font-size-xxl: 24px;    /* 模块标题 */
```

### 组件库

#### 基础组件 (Base)
- **BaseCard** - 统一的卡片容器，支持阴影和圆角
- **BaseButton** - 多类型按钮，支持加载状态和图标
- **BaseInput** - 表单输入框，支持验证状态显示
- **BaseTable** - 数据表格，支持分页和操作列

#### 通用组件 (Common)
- **ChartCanvas** - Canvas图表组件，支持5种图表类型
- **MessageToast** - 全局消息提示系统
- **PageTransition** - 页面切换过渡动画
- **LoadingIndicator** - 多种样式的加载指示器
- **HealthScore** - 健康评分可视化组件

#### 表单组件 (Form)
- **FormValidator** - 实时表单验证组件

## 🚀 核心功能

### 1. 认证管理 (AuthModule)
- ✅ 用户登录/注册/登出
- ✅ 自定义 token 验证（后端提供 `GET /auth/validate/{token}`）
- ✅ 认证记录管理
- ✅ 安全会话管理

### 2. 用户管理 (UserModule)
- ✅ 用户CRUD操作
- ✅ 高级搜索筛选（姓名/邮箱/电话/角色/状态）
- ✅ 分页表格展示
- ✅ 批量操作（激活/停用/删除）
- ✅ 模态框表单验证

### 3. 系统概览 (Dashboard)
- ✅ 实时统计数据展示
- ✅ 用户增长趋势图表
- ✅ 快速入口导航
- ✅ 系统健康状态监控
- ✅ 近期活动时间线

### 4. 数据可视化
- ✅ **ChartCanvas组件**：支持柱状图、折线图、饼图、环形图、面积图
- ✅ **实时数据更新**：响应式数据绑定
- ✅ **交互式图表**：悬停提示、图例控制
- ✅ **主题适配**：自动适配紫色主题

### 5. 交互体验优化
- ✅ **全局消息系统**：5种消息类型（成功/错误/警告/信息/加载）
- ✅ **表单验证**：实时验证，支持多种验证规则
- ✅ **页面过渡**：流畅的路由切换动画
- ✅ **加载状态**：多种加载指示器和进度显示

### 6. 无障碍访问 (A11y)
- ✅ **键盘导航**：Tab键完整导航支持
- ✅ **焦点管理**：模态框自动焦点陷阱
- ✅ **语义化HTML**：完整的ARIA标签
- ✅ **屏幕阅读器**：NVDA/JAWS/VoiceOver兼容
- ✅ **高对比度**：支持系统偏好设置

## 📱 响应式适配

### 断点设计
- **桌面端** (>1024px)：完整管理后台布局，侧边栏导航
- **平板端** (768px-1024px)：可折叠侧边栏，自适应布局
- **移动端** (<768px)：底部Tab导航，单列布局优化

### 移动端特性
- **触摸友好**：最小44px触摸目标
- **手势支持**：长按、滑动等手势操作
- **底部导航**：核心入口快速访问（后台与个人端布局不同）
- **垂直布局**：移动优先的垂直设计

## ⚙️ 安装和运行

### 环境要求
- **Node.js** >= 16.0.0
- **npm** >= 8.0.0 或 **yarn** >= 1.22.0

### 安装依赖
```bash
# 使用 npm
npm install

# 或使用 yarn
yarn install
```

### 开发环境运行
```bash
# 使用 npm
npm run dev

# 或使用 yarn
yarn dev
```
启动后访问 `http://localhost:5173/`

### 构建生产版本
```bash
# 使用 npm
npm run build

# 或使用 yarn
yarn build
```

### 预览生产版本
```bash
# 使用 npm
npm run preview

# 或使用 yarn
yarn preview
```

## 🔧 配置说明

### 后端API配置
默认连接到 `http://localhost:8080/api`

如需修改，请编辑 `src/shared/http.js`：
```javascript
const http = axios.create({
  baseURL: 'http://your-backend-url/api', // 修改此处
  timeout: 10000,
});
```

### 环境变量
创建 `.env` 文件进行配置：
```env
# API基础URL
VITE_API_BASE_URL=http://localhost:8080/api

# 应用标题
VITE_APP_TITLE=Ohelp2025

# 开发环境标识
VITE_ENV=development
```

## 📚 开发指南

### 开发原则
#### 后端即宪法原则
- ❌ 前端**不得创造、推导、重组业务语义**
- ✅ 严格按照后端接口文档生成代码
- ✅ 一个后端Controller = 一个前端模块
- ✅ 一个后端接口 = 一个前端方法

#### 模块化铁律
- ✅ **一个Controller = 一个api文件**
- ✅ **一个接口 = 一个前端方法**
- ✅ ❌ 禁止跨模块API调用
- ✅ ❌ 禁止前端私自添加字段

#### API层铁律
- ✅ URL、HTTP方法、字段名**必须100%一致**
- ✅ 每个模块有独立的types.js定义
- ✅ ❌ 禁止使用any类型

### 组件使用示例

#### 图表组件
```vue
<template>
  <ChartCanvas
    type="line"
    :data="chartData"
    width="100%"
    height="300"
    :show-legend="true"
  />
</template>

<script>
export default {
  data() {
    return {
      chartData: [
        { label: '1月', value: 100 },
        { label: '2月', value: 120 },
        // ...
      ]
    }
  }
}
</script>
```

#### 表单验证
```vue
<template>
  <FormValidator
    v-model="formData"
    :rules="validationRules"
    :field-labels="fieldLabels"
    @validate="handleValidate"
  >
    <BaseInput
      v-model="formData.name"
      label="姓名"
      @blur="onFieldBlur('name')"
    />
  </FormValidator>
</template>

<script>
export default {
  data() {
    return {
      formData: { name: '' },
      validationRules: {
        name: [
          { name: 'required', message: '请输入姓名' },
          { name: 'minLength', minLength: 2, message: '姓名至少2个字符' }
        ]
      },
      fieldLabels: { name: '姓名' }
    }
  },
  methods: {
    onFieldBlur(field) {
      this.$refs.validator.onFieldBlur(field)
    },
    handleValidate(isValid, errors) {
      console.log('验证结果:', isValid, errors)
    }
  }
}
</script>
```

#### 消息提示
```vue
<script>
import { MessageService } from '@/shared/message'

export default {
  methods: {
    showSuccess() {
      MessageService.success('操作成功！')
    },
    showError() {
      MessageService.error('操作失败，请重试')
    }
  }
}
</script>
```

## 🧪 测试与质量

### 代码质量
- **ESLint配置**：Vue 3 + JavaScript最佳实践
- **Prettier配置**：统一的代码格式化规范
- **Type Checking**：JSDoc注释提供类型提示

### 浏览器兼容性
- ✅ Chrome 70+
- ✅ Firefox 70+
- ✅ Safari 12+
- ✅ Edge 79+
- ✅ 移动端浏览器 (iOS Safari, Chrome Mobile)

### 无障碍标准
- ✅ **WCAG 2.1 AA** 符合度
- ✅ 键盘导航完整支持
- ✅ 屏幕阅读器兼容
- ✅ 高对比度模式支持

## 🔒 安全特性

- **XSS防护**：Vue模板编译自动转义
- **CSRF防护**：HTTP Only Cookies
- **内容安全策略**：CSP头部配置
- **HTTPS强制**：生产环境HTTPS
- **依赖安全**：定期依赖更新和审计

## 📈 性能优化

### 构建优化
- **代码分割**：路由懒加载
- **资源压缩**：Gzip压缩
- **图片优化**：WebP格式支持
- **缓存策略**：长期缓存和版本控制

### 运行时优化
- **虚拟滚动**：大数据量列表优化
- **防抖优化**：搜索和验证防抖
- **内存管理**：组件卸载时清理事件监听器
- **GPU加速**：CSS transform动画优化

## 🤝 贡献指南

### 代码规范
1. 遵循现有的代码结构和命名规范
2. 使用ESLint和Prettier进行代码检查
3. 提交前运行测试确保功能正常
4. 详细描述Pull Request的目的和更改内容

### 开发流程
1. Fork项目到个人仓库
2. 创建功能分支：`git checkout -b feature/new-feature`
3. 提交更改：`git commit -m 'Add new feature'`
4. 推送分支：`git push origin feature/new-feature`
5. 创建Pull Request

## 📞 技术支持

### 问题反馈
- **GitHub Issues**：提交bug报告和功能请求
- **项目维护**：定期更新和bug修复
- **社区支持**：Vue.js官方社区和文档

### 更新计划
- 🔄 **定期维护**：依赖更新和安全补丁
- 🚀 **功能增强**：基于用户反馈的新功能
- 📱 **移动优化**：持续改进移动端体验
- ♿ **无障碍提升**：进一步完善无障碍访问

## 📄 开源协议

本项目采用 **MIT License** 开源协议。

## 🙏 致谢

感谢以下开源项目的支持：
- [Vue.js](https://vuejs.org/) - 渐进式JavaScript框架
- [Vite](https://vitejs.org/) - 下一代前端构建工具
- [Axios](https://axios-http.com/) - HTTP客户端库
- [Vue Router](https://router.vuejs.org/) - 官方路由管理器

---

<div align="center">

## 🎉 项目状态

**✅ 已完成并可投入生产使用**

**开发周期**：约20个工作日

**代码质量**：高质量、可维护、可扩展

**用户体验**：优秀，满足适老化和专业管理需求

**技术先进性**：采用最新前端技术和最佳实践

---

**[开始使用](#安装和运行) | [查看演示](http://localhost:5173/) | [技术文档](#开发指南)**

</div>
